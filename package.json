{
  "name": "server",
  "version": "1.0.0",
  "description": "",
  "main": "server/app.js",
  "scripts": {
    "clean": "rm -rf build && mkdir build",
    "build-server" : "babel -d ./build ./serve -s",
    // "build": "babel server -out-dir build",
    "build": "npm run clean && npm run build-css && npm run build-server",
    "start": "node --debug ./build/app.js",
    "start:dev": "nodemon --exec babel-node server/app",	
    "test": "nyc --require @babel/register --reporter=lcov --reporter=text mocha --exit --timeout 90000 --recursive",
    "coverage": "nyc report --reporter=text-lcov | coveralls",
    "coveralls": "nyc --reporter=lcov --reporter=text-lcov npm test",
    "heroku-postbuild": "npm run build && npm run start"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "bcrypt": "^3.0.6",
    "body-parser": "^1.19.0",
    "chai": "^4.2.0",
    "chai-http": "^4.3.0",
    "cloudinary": "^1.14.0",
    "connect-multiparty": "^2.2.0",
    "dotenv": "^8.0.0",
    "express": "^4.17.1",
    "jsonwebtoken": "^8.5.1",
    "mocha": "^6.1.4",
    "morgan": "^1.9.1",
    "nyc": "^14.1.1"
  },
  "devDependencies": {
    "@babel/cli": "^7.4.4",
    "@babel/core": "^7.4.5",
    "@babel/node": "^7.4.5",
    "@babel/preset-env": "^7.4.5",
    "@babel/register": "^7.4.4",
    "coveralls": "^3.0.3",
    "eslint": "^5.16.0",
    "eslint-config-airbnb-base": "^13.1.0",
    "eslint-plugin-import": "^2.17.3",
    "mocha-lcov-reporter": "^1.3.0"
  }
}
